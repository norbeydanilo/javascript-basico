/*
 * Constantes.
 * Son datos que no cambian en la ejecución del código, se definen de la siguiente manera.
 *  const NOMBRE_CONSTANTE = valorConstante
 * Por ejemplo, en mátematicas el valor de PI no cambia, es constante. 
 * Vea el siguiente código
 */

const PI = 3.1416
// Otra manera de concatenar texto es: `Texto ${variable / constante}`
console.log(`El valor de PI es: ${PI}`)

const minuto = 60
const segundo = 60
const hora = 24

console.log(`Un minuto tiene ${segundo} segundos, una hora tiene ${minuto} minutos y un día tiene ${hora} horas`)

console.log(`Una hora tiene: ${hora} minutos`)

console.log(`Un minuto tiene: ${segundo} segundos`) 